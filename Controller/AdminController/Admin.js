import mysql from 'mysql';

export const CreateAdmin = (firstName, lastName, username, email, password, countryCode, 
    regionCode, inputPhoneNumber, nationalNumber, e164Format, orginalFOrmat, internationalFormat,
     isValidNumber, isValidNumberForRegion, address, city, state, country, street, addressTypeId) =>{

}